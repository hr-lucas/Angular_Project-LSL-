import { Validator, AbstractControl } from '@angular/forms';
export declare class InscricaoEstadualValidator implements Validator {
    validate(c: AbstractControl): {
        [key: string]: any;
    };
}
